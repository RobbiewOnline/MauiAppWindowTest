<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppWindowTest.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit" />

            <Label
                Text="Will transition to BLUE automatically in 5 seconds!!"
                Style="{StaticResource Headline}" />

            <Button
                Text="Manual - Push Fuchsia via nav" 
                BackgroundColor="Fuchsia"
                TextColor="White"
                Clicked="FuchsiaPushOntoNavClicked"
                HorizontalOptions="Fill" />

            <Button
                Text="Manual - Red Page via window" 
                BackgroundColor="Red"
                TextColor="White"
                Clicked="RedPageViaWindowClicked"
                HorizontalOptions="Fill" />

            <Button
                Text="Manual - Green Page via window" 
                BackgroundColor="Green"
                TextColor="White"
                Clicked="GreenPageViaWindowClicked"
                HorizontalOptions="Fill" />


        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
