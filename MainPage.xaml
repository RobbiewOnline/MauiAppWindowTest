<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppWindowTest.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                Text="Will transition automatically in 5 seconds!!"
                Style="{StaticResource Headline}" />

            <Button
                Text="Manual - Push Fuchsia via nav" 
                Clicked="FuchsiaPushOntoNavClicked"
                HorizontalOptions="Fill" />

            <Button
                Text="Manual - Red Page via window" 
                Clicked="RedPageViaWindowClicked"
                HorizontalOptions="Fill" />

            <Button
                Text="Manual - Green Page via window" 
                Clicked="GreenPageViaWindowClicked"
                HorizontalOptions="Fill" />


        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
